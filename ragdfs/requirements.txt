# =============================================================================
# RAG SIKA - Fichas de Datos de Seguridad
# Python 3.14 | Generado: 2026-05-26
# Instalación: pip install -r requirements.txt
# =============================================================================

# --- Extracción PDF (texto, tablas, imágenes) ---
PyMuPDF==1.27.2.3
pdfplumber==0.11.9
pytesseract==0.3.13
Pillow==12.2.0

# --- PDF → Markdown con preservación de estructura ---
# ⚠️  Instalar DESPUÉS con --no-deps para evitar conflicto de Pillow:
#     pip install marker-pdf==1.10.2 --no-deps
# marker-pdf==1.10.2

# --- Cliente Python para Ollama (LLM local) ---
ollama==0.6.2

# --- LangChain — orquestación del pipeline RAG ---
langchain==1.3.1
langchain-community==0.4.2
langchain-ollama==1.1.0
langchain-chroma==1.1.0

# --- Vector store local (persiste en disco, sin servidor) ---
chromadb==1.5.9

# --- Evaluación del RAG (métricas: faithfulness, relevancy, recall) ---
ragas==0.4.3

# --- Notebooks, análisis y utilidades ---
jupyter==1.1.1
pandas==3.0.3
tqdm==4.67.3
python-dotenv==1.2.2
tabulate==0.9.0
